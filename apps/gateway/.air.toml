root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/gateway ./apps/gateway/main.go"
  entrypoint = "./tmp/gateway"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "deployments", ".git"]
  delay = 1000
  stop_on_error = true
  rerun = true

[log]
  time = true
